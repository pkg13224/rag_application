
# just to avoid code duplication so every time we will reference the nodes we will reference the 
# actually const and if we wan to change the ane from some reasoen we only need 
# to change it in one place.
RETRIEVE = "retrieve"
GRAGE_DOCUMENTS = 'grade_documents'
GENERATE = 'generate'
WEBSEARCH = 'websearch'